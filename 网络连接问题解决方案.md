# Android网络连接问题解决方案

**问题**: `SocketTimeoutException: failed to connect to /192.168.8.107`

**原因**: Android模拟器的网络配置与真机不同，无法直接访问宿主机的局域网IP。

---

## 🔧 解决方案

### 方案1：使用模拟器专用地址（已应用）✅

**适用场景**: 使用Android模拟器 + 后端运行在本机

**配置**:
```kotlin
private const val BASE_URL = "http://10.0.2.2:8000/"
```

**说明**:
- `10.0.2.2` 是Android模拟器的特殊地址，指向宿主机的 localhost
- 相当于访问你电脑的 `localhost:8000` 或 `127.0.0.1:8000`

**前提条件**:
1. 后端服务必须在你的电脑上运行
2. 后端监听在 `0.0.0.0:8000` 或 `127.0.0.1:8000`

**验证步骤**:
```bash
# 1. 确认后端正在运行
curl http://localhost:8000/docs

# 2. 重新编译并运行Android应用
# 3. 查看日志，应该能成功连接
```

---

### 方案2：使用真机 + 局域网IP

**适用场景**: 使用Android真机测试

**配置**:
```kotlin
private const val BASE_URL = "http://192.168.8.107:8000/"
```

**前提条件**:
1. 手机和电脑连接到同一个WiFi网络
2. 后端服务监听在 `0.0.0.0:8000`（不能是127.0.0.1）
3. 防火墙允许8000端口访问

**获取电脑IP地址**:
```bash
# Windows
ipconfig

# 查找 "无线局域网适配器 WLAN" 下的 IPv4 地址
# 例如: 192.168.8.107
```

**后端启动命令**:
```bash
# 确保监听所有网络接口
uvicorn main:app --host 0.0.0.0 --port 8000
```

---

### 方案3：使用Railway生产环境

**适用场景**: 后端已部署到Railway

**配置**:
```kotlin
private const val BASE_URL = "https://ai-bookmark-production.up.railway.app/"
```

**优点**:
- 模拟器和真机都能访问
- 不依赖本地后端
- 测试真实的生产环境

**缺点**:
- 需要网络连接
- 调试不如本地方便

---

### 方案4：启用Mock模式（无需后端）

**适用场景**: 后端暂时不可用，只测试前端UI

**配置**:
```kotlin
private const val MOCK_API = true  // 改为 true
private const val BASE_URL = "http://10.0.2.2:8000/"  // 任意值都可以
```

**说明**:
- 所有API请求返回预设的Mock数据
- 不需要真实的后端服务
- 适合UI开发和测试

---

## 📋 当前配置建议

根据你的测试环境选择：

### 开发阶段（推荐）
```kotlin
private const val MOCK_API = false
private const val BASE_URL = "http://10.0.2.2:8000/"  // 模拟器
// private const val BASE_URL = "http://192.168.8.107:8000/"  // 真机
```

### 生产测试
```kotlin
private const val MOCK_API = false
private const val BASE_URL = "https://ai-bookmark-production.up.railway.app/"
```

### UI开发（无后端）
```kotlin
private const val MOCK_API = true
private const val BASE_URL = "http://10.0.2.2:8000/"
```

---

## 🐛 常见问题排查

### 1. 模拟器仍然连接失败

**检查后端是否运行**:
```bash
# 在浏览器打开
http://localhost:8000/docs

# 或使用curl
curl http://localhost:8000/api/v1/auth/login
```

**检查端口是否被占用**:
```bash
# Windows
netstat -ano | findstr :8000

# 如果被占用，杀掉进程或换端口
```

### 2. 真机连接失败

**检查防火墙**:
- Windows: 控制面板 → 防火墙 → 允许应用通过防火墙
- 添加Python或uvicorn的入站规则

**检查后端监听地址**:
```bash
# 错误：只监听本地
uvicorn main:app --host 127.0.0.1 --port 8000

# 正确：监听所有接口
uvicorn main:app --host 0.0.0.0 --port 8000
```

**验证网络连通性**:
```bash
# 在手机上用浏览器访问
http://192.168.8.107:8000/docs

# 如果能打开，说明网络正常
```

### 3. 超时时间太短

如果网络较慢，可以增加超时时间：
```kotlin
private const val TIMEOUT_SECONDS = 60L  // 改为60秒
```

### 4. HTTPS证书问题

如果使用Railway但遇到SSL错误，检查：
```kotlin
// 确保URL是 https:// 而不是 http://
private const val BASE_URL = "https://ai-bookmark-production.up.railway.app/"
```

---

## 🧪 测试步骤

### 1. 验证后端可访问
```bash
# 本地后端
curl http://localhost:8000/docs

# Railway后端
curl https://ai-bookmark-production.up.railway.app/docs
```

### 2. 修改NetworkModule.kt
根据你的测试环境选择合适的BASE_URL

### 3. 清理并重新编译
```bash
# 在Android Studio中
Build → Clean Project
Build → Rebuild Project
```

### 4. 运行应用
- 查看Logcat日志
- 搜索 "OkHttp" 或 "okhttp"
- 确认连接成功

### 5. 测试登录
- 用户名: test
- 密码: test123
- 观察是否能成功登录

---

## 📝 快速切换配置

为了方便在不同环境间切换，可以使用BuildConfig：

```kotlin
// build.gradle.kts (app level)
android {
    buildTypes {
        debug {
            buildConfigField("String", "API_BASE_URL", "\"http://10.0.2.2:8000/\"")
        }
        release {
            buildConfigField("String", "API_BASE_URL", "\"https://ai-bookmark-production.up.railway.app/\"")
        }
    }
}

// NetworkModule.kt
private val BASE_URL = BuildConfig.API_BASE_URL
```

这样Debug版本自动用本地，Release版本用生产环境。

---

**最后更新**: 2026-03-01  
**状态**: 已修改为模拟器专用地址 `10.0.2.2`
