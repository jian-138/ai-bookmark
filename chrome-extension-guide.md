# Chrome扩展使用指南

## 功能概述

AI书签收藏助手Chrome扩展提供了智能收藏功能，支持普通网页和微信公众号文章的收藏与AI分析。

## 安装方法

1. 打开Chrome浏览器
2. 访问 `chrome://extensions`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `chrome-extension` 文件夹

## 主要功能

### 1. 智能收藏
- 在任意网页选中文本，页面上会显示浮动收藏按钮
- 点击按钮即可快速收藏选中的内容
- 支持键盘快捷键 `Ctrl+Shift+S` 进行收藏

### 2. 右键菜单
- 选中文本后，右键菜单中会出现"收藏选中内容到AI书签"
- 点击该选项可直接收藏

### 3. 微信公众号文章优化
- 在微信公众号文章页面，扩展会自动识别为微信环境
- 收藏操作将使用异步API，实现静默收藏（无提示信息）
- 更适合在微信内置浏览器中使用

### 4. 用户管理
- 使用测试账号登录：`test` / `test123`
- 收藏的内容会同步到服务器并进行AI分析

## 微信公众号文章收藏流程

1. 在微信中打开公众号文章
2. 选择想要收藏的文字内容
3. 页面上会出现浮动收藏按钮
4. 点击按钮，收藏将静默提交到后台
5. AI会在后台分析内容，完成后可在扩展中查看结果

## 离线功能

- 当网络不可用时，收藏内容会被暂存到本地队列
- 网络恢复后，可通过点击同步按钮自动上传

## API端点说明

- **同步收藏**：`/api/v1/collect` - 立即响应，等待AI分析完成
- **异步收藏**：`/api/v1/collect-async` - 立即返回，AI分析在后台进行（用于微信环境）

## 使用技巧

1. **最大化收藏效率**：
   - 选择有意义的文本段落
   - 避免选择过短的文本（少于10个字符）

2. **微信环境下的使用**：
   - 由于微信浏览器限制，扩展会在后台静默提交收藏
   - 不会弹出提示框，但收藏仍然成功

3. **快捷键**：
   - `Ctrl+Shift+S`：收藏当前选中文本

## 故障排除

### 无法连接到服务器
- 检查后端服务是否运行
- 确认 `background.js` 中的API地址是否为当前网络IP地址

### 浮动按钮不显示
- 检查页面是否允许内容脚本运行
- 确认已启用扩展的必要权限

### 微信文章收藏失败
- 确认网络连接正常
- 检查是否已登录

## 技术架构

- **内容脚本**：`content.js` - 捕获用户选择的文本
- **后台脚本**：`background.js` - 处理API通信和数据管理
- **弹出界面**：`popup.html/css/js` - 用户交互界面
- **配置文件**：`manifest.json` - 扩展配置和权限声明