# 启动后端服务指南

## 🚀 快速启动（推荐）

### 方法1：直接启动（最简单）

在项目根目录打开终端，运行：

```bash
# 激活虚拟环境（如果有的话）
.venv\Scripts\activate

# 启动后端服务
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 方法2：使用Python模块方式

```bash
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 📋 完整步骤（首次启动）

### 1. 检查Python环境

```bash
# 检查Python版本（需要3.11+）
python --version

# 如果没有Python，需要先安装
```

### 2. 激活虚拟环境

```bash
# Windows
.venv\Scripts\activate

# 如果虚拟环境不存在，先创建
python -m venv .venv
.venv\Scripts\activate
```

### 3. 安装依赖（首次需要）

```bash
# 安装所有依赖
pip install -r requirements.txt

# 或者手动安装核心依赖
pip install fastapi uvicorn python-dotenv requests beautifulsoup4
```

### 4. 启动服务

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 🎯 启动参数说明

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

- `main:app` - main.py文件中的app对象
- `--host 0.0.0.0` - 监听所有网络接口（允许外部访问）
- `--port 8000` - 使用8000端口
- `--reload` - 代码修改后自动重启（开发模式）

---

## ✅ 验证服务是否启动成功

### 1. 查看终端输出

应该看到类似这样的输出：
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [xxxxx] using WatchFiles
INFO:     Started server process [xxxxx]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### 2. 在浏览器中访问

打开浏览器，访问以下地址：

- **API文档**: http://localhost:8000/docs
- **根路径**: http://localhost:8000/
- **健康检查**: http://localhost:8000/

### 3. 测试登录接口

```bash
# 使用curl测试
curl -X POST http://localhost:8000/api/v1/auth/login ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"test\",\"password\":\"test123\"}"

# 应该返回类似这样的JSON
# {"success":true,"token":"test_token_...","user_id":"usr_...","message":"登录成功"}
```

---

## 🐛 常见问题

### 问题1：端口被占用

**错误信息**: `Address already in use`

**解决方案**:
```bash
# 查找占用8000端口的进程
netstat -ano | findstr :8000

# 杀掉进程（替换xxxxx为实际的PID）
taskkill /PID xxxxx /F

# 或者换个端口
uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```

### 问题2：找不到uvicorn命令

**错误信息**: `'uvicorn' is not recognized`

**解决方案**:
```bash
# 确保虚拟环境已激活
.venv\Scripts\activate

# 重新安装uvicorn
pip install uvicorn

# 或使用Python模块方式
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 问题3：缺少依赖包

**错误信息**: `ModuleNotFoundError: No module named 'xxx'`

**解决方案**:
```bash
# 安装缺失的包
pip install xxx

# 或重新安装所有依赖
pip install -r requirements.txt
```

### 问题4：虚拟环境激活失败

**错误信息**: `无法加载文件 xxx\activate.ps1`

**解决方案**:
```bash
# 使用cmd而不是PowerShell
# 或者在PowerShell中执行
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 然后再激活
.venv\Scripts\activate
```

---

## 🔧 开发模式 vs 生产模式

### 开发模式（当前）
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```
- 代码修改自动重启
- 详细的错误信息
- 适合本地开发

### 生产模式（部署时）
```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --workers 4
```
- 多进程运行
- 更高性能
- 适合生产环境

---

## 📝 快速命令备忘

```bash
# 1. 激活虚拟环境
.venv\Scripts\activate

# 2. 启动后端
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 3. 在浏览器打开API文档
start http://localhost:8000/docs

# 4. 停止服务
# 按 Ctrl+C
```

---

## 🌐 访问地址汇总

启动后，可以通过以下地址访问：

| 地址 | 说明 | 用途 |
|------|------|------|
| http://localhost:8000 | 本机访问 | 浏览器测试 |
| http://127.0.0.1:8000 | 本机访问（备用） | 浏览器测试 |
| http://10.0.2.2:8000 | Android模拟器访问 | 模拟器测试 |
| http://192.168.x.x:8000 | 局域网访问 | 真机测试 |
| http://localhost:8000/docs | API文档 | 查看所有接口 |

---

## 🎉 启动成功后

1. **保持终端窗口打开**（不要关闭）
2. **在Android Studio中运行应用**
3. **查看两边的日志**，确认连接成功
4. **开始测试**！

---

**最后更新**: 2026-03-01  
**状态**: 后端服务配置完成，等待启动
