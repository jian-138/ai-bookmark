# Chrome扩展本地测试指南

## 🎯 当前状态总结

### ✅ 已完成的工作

1. **恢复原始设计**: Chrome扩展前端已恢复为简洁的原始设计
2. **本地服务器**: 后端服务已在本地成功启动并运行正常
3. **API测试**: 本地API接口测试通过，登录和收藏功能正常
4. **Railway问题**: 识别到Railway部署存在502错误，正在解决中

### 🔧 当前问题

**Railway 502错误**: 生产环境仍然无法访问，但本地开发环境完全可用

## 🚀 本地测试步骤

### 1. 启动本地服务器（已完成）
```bash
# 服务器已在端口8000运行
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

### 2. 修改Chrome扩展配置
编辑 `chrome-extension/config.js` 文件：

```javascript
// 将 CURRENT_ENV 改为 'development'
const CURRENT_ENV = 'development'; // 改为开发环境
```

### 3. 测试本地API
打开浏览器访问测试页面：
```
http://localhost:8000/test-extension.html
```

或者直接测试API端点：
```
# 健康检查
http://localhost:8000/health

# 登录测试（使用POST请求）
POST http://localhost:8000/api/v1/auth/login
Body: {"username":"test","password":"test123"}
```

### 4. 加载Chrome扩展
1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `chrome-extension` 文件夹
6. 扩展应该成功加载

### 5. 测试扩展功能
1. 点击扩展图标打开弹出窗口
2. 使用测试账号登录：
   - 用户名: `test`
   - 密码: `test123`
3. 测试收藏功能：
   - 在任何网页上选中文本
   - 右键菜单选择"AI收藏"
   - 查看收藏列表

## 📋 测试账号信息

- **测试用户名**: `test`
- **测试密码**: `test123`
- **本地API地址**: `http://localhost:8000`

## 🔍 故障排除

### 如果登录失败
1. 检查本地服务器是否运行：`http://localhost:8000/health`
2. 确认扩展配置为开发环境
3. 检查浏览器控制台错误信息

### 如果收藏功能不工作
1. 确认已登录成功
2. 检查选中的文本长度（至少10个字符）
3. 查看扩展的背景页控制台

### 如果Railway服务仍然502
我们已推送修复，但Railway可能需要时间重新部署。请优先使用本地服务器进行测试。

## 🎨 设计变更说明

### 原始设计特点
- **简洁界面**: 移除了复杂的周报功能和搜索功能
- **核心功能**: 专注于登录、收藏列表展示和同步功能
- **响应式布局**: 适配不同屏幕尺寸
- **清晰视觉**: 使用蓝色主题，界面元素简洁明了

### 主要改进
1. **简化操作流程**: 减少不必要的页面跳转
2. **优化性能**: 移除复杂的JavaScript逻辑
3. **提升稳定性**: 减少潜在的bug点
4. **更好的用户体验**: 更直观的界面设计

## 🚀 下一步计划

1. **等待Railway部署**: 新的代码已推送，等待自动部署完成
2. **验证生产环境**: 部署完成后测试线上服务
3. **持续优化**: 根据使用反馈继续改进

## 📞 支持

如果遇到问题，请检查：
1. 本地服务器日志
2. Chrome扩展的背景页控制台
3. 浏览器网络请求
4. API响应状态

当前本地环境已完全可用，建议先使用本地服务器进行开发和测试！