# 周报功能快速启动指南

## 🚀 快速开始

### 1. 启动后端服务

```bash
# 在项目根目录执行
python main.py
```

服务将在 `http://localhost:8000` 启动

### 2. 打开演示界面

在浏览器中打开：
```
weekly_favorite_demo.html
```

### 3. 测试功能

#### 方法一：使用演示界面
1. 点击关键词云中的标签进行搜索
2. 在搜索框输入关键词
3. 查看搜索结果
4. 点击"收藏"按钮添加收藏

#### 方法二：使用 API 测试工具（如 Postman）

**添加收藏**
```bash
POST http://localhost:8000/api/v1/weekly/favorites
Content-Type: application/json

{
  "user_id": "user_123",
  "collection_id": "col_123",
  "keywords": ["Python", "编程"],
  "favorite_note": "测试收藏"
}
```

**搜索关键词**
```bash
POST http://localhost:8000/api/v1/weekly/keywords/search
Content-Type: application/json

{
  "user_id": "user_123",
  "keyword": "Python",
  "exact_match": false,
  "page": 1,
  "page_size": 20
}
```

**获取所有关键词**
```bash
GET http://localhost:8000/api/v1/weekly/keywords?user_id=user_123
```

**获取统计信息**
```bash
GET http://localhost:8000/api/v1/weekly/keywords/statistics?user_id=user_123
```

### 4. 运行测试

#### 简化测试（推荐）
```bash
python test_weekly_simple.py
```

#### 集成测试（需要先启动服务）
```bash
python test_weekly_favorite_integration.py
```

## 📋 API 接口列表

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/v1/weekly/favorites` | POST | 添加周报收藏 |
| `/api/v1/weekly/favorites` | GET | 获取收藏列表 |
| `/api/v1/weekly/favorites/{id}` | DELETE | 移除收藏 |
| `/api/v1/weekly/keywords/search` | POST | 关键词搜索 |
| `/api/v1/weekly/keywords` | GET | 获取所有关键词 |
| `/api/v1/weekly/keywords/statistics` | GET | 获取统计信息 |
| `/api/v1/weekly/reports/search` | GET | 搜索周报 |

## 🔧 数据管理

### 备份数据
```bash
# 备份所有数据
python backend/app/services/data_backup.py --backup

# 备份指定用户
python backend/app/services/data_backup.py --backup --user-id user_123

# 指定备份目录
python backend/app/services/data_backup.py --backup --backup-dir my_backups
```

### 清理数据
```bash
# 清理过期数据
python backend/app/services/data_backup.py --cleanup
```

## 📊 功能特性

✅ **智能关键词提取**
- 使用 AI 自动提取关键词
- 支持批量提取和词频统计

✅ **高效搜索**
- 精确匹配：直接查找关键词
- 模糊搜索：智能匹配相似词
- 原文搜索：在收藏内容中搜索

✅ **1 年数据保存**
- 自动设置过期时间
- 定期清理过期数据
- 完整备份机制

✅ **友好界面**
- 关键词云展示
- 实时搜索反馈
- 收藏管理便捷

## 🐛 故障排查

### 问题：无法连接到服务器
**解决方案**：
1. 确保后端服务已启动：`python main.py`
2. 检查端口 8000 是否被占用
3. 查看控制台错误信息

### 问题：AI 分析失败
**解决方案**：
1. 检查网络连接
2. 验证 API 密钥配置
3. 查看 `ai/analyze.py` 中的错误日志

### 问题：数据库错误
**解决方案**：
1. 确保数据库已正确初始化
2. 检查 `backend/app/models.py` 中的模型定义
3. 运行数据库迁移（如需要）

## 📚 详细文档

- 完整功能说明：`周报收藏功能实现文档.md`
- 测试文件：`test_weekly_simple.py`
- 演示代码：`weekly_favorite_demo.html`

## 🎯 下一步

1. ✅ 启动服务并测试基本功能
2. ✅ 运行测试验证功能正常
3. ✅ 根据需求调整配置
4. ✅ 集成到现有系统

祝你使用愉快！🎉
