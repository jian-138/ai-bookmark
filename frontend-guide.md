# AI书签收藏助手 - 前端简易入口使用指南

## 快速开始

### 1. 启动后端服务
双击 `start_server.bat` 文件启动后端服务，或在命令行中运行：
```bash
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### 2. 访问简易入口
打开浏览器，访问以下任一方式：
- 直接打开 `index.html` 文件（双击即可）
- 或访问 `http://10.76.244.132:8000` （移动端也可以访问）

### 3. 安装Chrome扩展
1. 打开Chrome浏览器
2. 访问 `chrome://extensions`
3. 开启"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择 `chrome-extension` 文件夹

### 4. 使用服务
1. 点击浏览器右上角的扩展图标
2. 使用测试账号登录：`test` / `test123`
3. 在任意网页选中文本，右键选择"收藏选中内容到AI书签"

## 功能特点

### 简易可视化入口
- 直观的卡片式界面展示各项功能
- 清晰的使用步骤指引
- 一键访问Chrome扩展界面
- 移动端友好的设计

### 主要功能
- **智能收藏**：一键收藏网页选中内容
- **AI分析**：自动分析内容，提取关键词、分类和摘要
- **微信支持**：特别优化微信公众号文章收藏
- **多端同步**：支持移动端访问和管理

## 移动端访问

在移动设备上打开浏览器，访问：
```
http://192.168.41.132:8000
```

确保移动设备与运行服务器的电脑在同一网络中。

### 微信内置浏览器访问

专门针对微信内置浏览器优化的页面：
```
http://192.168.41.132:8000/wechat
```

此页面针对微信环境进行了特殊优化，提供更好的使用体验。

## Chrome扩展使用

### 安装后首次使用
1. 点击扩展图标
2. 在弹出的窗口中输入测试账号：`test` / `test123`
3. 点击"登录"

### 收藏内容
1. 在网页上选中想要收藏的文字内容
2. 页面上会出现浮动收藏按钮，点击即可收藏
3. 或者右键点击选中区域，选择"收藏选中内容到AI书签"
4. 查看扩展图标上的收藏数量变化

### 微信公众号文章收藏（增强功能）
1. 在微信中打开公众号文章
2. 选择想要收藏的文字内容
3. 页面上会出现浮动收藏按钮
4. 点击按钮，收藏将静默提交到后台（无提示信息）
5. AI会在后台分析内容，完成后可在扩展中查看结果

### 管理收藏
1. 点击扩展图标
2. 查看"我的收藏"页面
3. 可以查看所有收藏的内容、AI分析结果等

### 使用技巧
- 使用快捷键 `Ctrl+Shift+S` 可快速收藏选中文本
- 扩展支持离线收藏功能，网络恢复后可自动同步
- 在微信环境中，收藏会自动使用异步API实现静默操作

## API文档

访问 `http://10.76.244.132:8000/docs` 查看完整的API文档。

## 故障排除

### 无法访问服务
- 确认后端服务已启动
- 检查防火墙设置是否阻止了8000端口
- 确认设备在同一网络中

### 扩展无法连接后端
- 检查 `chrome-extension/background.js` 中的API地址是否正确
- 确认后端服务正在运行

### 登录失败
- 确认使用测试账号：`test` / `test123`
- 检查后端服务是否正常运行

## 技术栈

- **前端**：Chrome扩展程序（HTML, CSS, JavaScript）
- **后端**：FastAPI（Python）
- **AI分析**：集成多种AI模型进行内容分析
- **数据存储**：内存存储（开发环境）

## 开发信息

项目负责人：石金来（前端开发）
- 负责pages页面文件夹内接口适配
- AI即时求助功能开发

如有问题请联系开发团队。